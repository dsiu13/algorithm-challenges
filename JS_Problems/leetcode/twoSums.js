let twoSums = (arr, target) => {
  let res = [];
  while (i < arr.length - 1) {
    i = 0
    arr.map(x + arr[i + 1] === target){
      if(x + arr[i + 1]){
          res.push(indexOf(x))
          res.push(indexOf(arr[i + 1]))
      }
    }
    i++
  }
}
